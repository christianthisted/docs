All authenticated actions require the `X-Api-Key` header. Your API key is a UUID tied to your company. Generate one in **Settings > API keys** in your Keen dashboard.

```bash
-H "X-Api-Key: YOUR_API_KEY"
```
